export enum group {
  'Extremo 1',
  'Extremo 2',
}

export enum response {
  'Muy de acuerdo',
  'De acuerdo',
  'En desacuerdo',
  'Muy en desacuerdo',
}

export const responses = Object.keys(response);
export const groups = Object.keys(group);
